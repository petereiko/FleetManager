@* @using FleetManager.Business.DataObjects
@model DriverDto
@{
    ViewData["Title"] = "Driver Profile - " + Model.FullName;
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">

<style>
    .driver-profile-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: #f8f9fa;
        min-height: 100vh;
    }

    .profile-header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 15px;
        padding: 30px;
        margin-bottom: 30px;
        color: white;
        position: relative;
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
    }

        .profile-header::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            transform: translate(50px, -50px);
        }

    .header-content {
        display: flex;
        align-items: center;
        gap: 30px;
        position: relative;
        z-index: 1;
    }

    .profile-photo-container {
        position: relative;
    }

    .profile-photo {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        border: 5px solid rgba(255, 255, 255, 0.3);
        object-fit: cover;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        transition: transform 0.3s ease;
    }

        .profile-photo:hover {
            transform: scale(1.05);
        }

    .status-badge {
        position: absolute;
        bottom: 10px;
        right: 10px;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        border: 3px solid white;
    }

    .status-active {
        background: #28a745;
    }

    .status-inactive {
        background: #dc3545;
    }

    .header-info h1 {
        margin: 0 0 10px 0;
        font-size: 2.2em;
        font-weight: 300;
    }

    .header-meta {
        display: flex;
        gap: 25px;
        flex-wrap: wrap;
        margin-top: 15px;
    }

    .meta-item {
        display: flex;
        align-items: center;
        gap: 8px;
        background: rgba(255, 255, 255, 0.15);
        padding: 8px 15px;
        border-radius: 20px;
        font-size: 0.9em;
    }

    .content-grid {
        display: grid;
        grid-template-columns: 1fr 350px;
        gap: 30px;
        margin-bottom: 30px;
    }

    .main-content {
        display: flex;
        flex-direction: column;
        gap: 25px;
    }

    .info-card {
        background: white;
        border-radius: 12px;
        padding: 25px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

        .info-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
        }

    .card-title {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 20px;
        font-size: 1.3em;
        font-weight: 600;
        color: #333;
        border-bottom: 2px solid #f1f3f4;
        padding-bottom: 10px;
    }

        .card-title i {
            color: #667eea;
            font-size: 1.2em;
        }

    .info-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 15px;
    }

    .info-item {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .info-label {
        font-size: 0.85em;
        font-weight: 600;
        color: #666;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .info-value {
        font-size: 1em;
        color: #333;
        font-weight: 500;
    }

    .sidebar {
        display: flex;
        flex-direction: column;
        gap: 25px;
    }

    .quick-stats {
        background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        color: white;
        border-radius: 12px;
        padding: 25px;
    }

    .stat-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    }

        .stat-item:last-child {
            border-bottom: none;
        }

    .documents-section {
        grid-column: 1 / -1;
    }

    .documents-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 20px;
        margin-top: 20px;
    }

    .document-card {
        background: white;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        transition: transform 0.2s ease;
    }

        .document-card:hover {
            transform: translateY(-3px);
        }

    .document-image {
        width: 100%;
        height: 200px;
        object-fit: cover;
        background: #f8f9fa;
    }

    .document-info {
        padding: 15px;
    }

    .document-name {
        font-weight: 600;
        color: #333;
        margin-bottom: 5px;
    }

    .no-image-placeholder {
        height: 200px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(45deg, #f8f9fa, #e9ecef);
        color: #6c757d;
        font-size: 1.1em;
    }

    .license-alert {
        padding: 15px;
        border-radius: 8px;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .license-warning {
        background: #fff3cd;
        border: 1px solid #ffeaa7;
        color: #856404;
    }

    .license-danger {
        background: #f8d7da;
        border: 1px solid #f5c6cb;
        color: #721c24;
    }

    .license-valid {
        background: #d1edff;
        border: 1px solid #bee5eb;
        color: #0c5460;
    }

    @@media (max-width: 768px) {
        .content-grid

    {
        grid-template-columns: 1fr;
    }

    .header-content {
        flex-direction: column;
        text-align: center;
    }

    .header-meta {
        justify-content: center;
    }

    .info-grid {
        grid-template-columns: 1fr;
    }

    }
</style>

<div class="driver-profile-container">
    <!-- License Status Alert -->
    @{
        var daysUntilExpiry = Model.LicenseExpiryDate.HasValue ?
        (Model.LicenseExpiryDate.Value - DateTime.Now).TotalDays : 0;
    }

    @if (Model.LicenseExpiryDate.HasValue)
    {
        @if (daysUntilExpiry <= 0)
        {
            <div class="license-alert license-danger">
                <i class="fas fa-exclamation-triangle"></i>
                <strong>License Expired:</strong> This driver's license expired on @Model.LicenseExpiryDate.Value.ToString("MM/dd/yyyy").
            </div>
        }
        else if (daysUntilExpiry <= 30)
        {
            <div class="license-alert license-warning">
                <i class="fas fa-exclamation-circle"></i>
                <strong>License Expiring Soon:</strong> This driver's license expires in @Math.Ceiling(daysUntilExpiry) days.
            </div>
        }
        else
        {
            <div class="license-alert license-valid">
                <i class="fas fa-check-circle"></i>
                <strong>License Valid:</strong> Expires on @Model.LicenseExpiryDate.Value.ToString("MM/dd/yyyy").
            </div>
        }
    }

    <!-- Profile Header -->
    <div class="profile-header">
        <div class="header-content">
            <div class="profile-photo-container">
                <img src="@Model.PassportFileName" alt="@Model.FullName" class="profile-photo"
                     onerror="this.src='data:image/svg+xml;base64,'" />
                <div class="status-badge @(Model.IsActive ? "status-active" : "status-inactive")"></div>
            </div>
            <div class="header-info">
                <h1>@Model.FullName</h1>
                <div class="header-meta">
                    <div class="meta-item">
                        <i class="fas fa-envelope"></i>
                        @Model.Email
                    </div>
                    <div class="meta-item">
                        <i class="fas fa-phone"></i>
                        @Model.PhoneNumber
                    </div>
                    <div class="meta-item">
                        <i class="fas fa-id-card"></i>
                        @Model.LicenseNumber
                    </div>
                    <div class="meta-item">
                        <i class="fas fa-calendar"></i>
                        Joined @Model.CreatedDate.ToString("MMM yyyy")
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="content-grid">
        <div class="main-content">
            <!-- Personal Information -->
            <div class="info-card">
                <div class="card-title">
                    <i class="fas fa-user"></i>
                    Personal Information
                </div>
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-label">Full Name</div>
                        <div class="info-value">@Model.FullName</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Date of Birth</div>
                        <div class="info-value">@Model.DateOfBirth.ToString("MMMM dd, yyyy")</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Gender</div>
                        <div class="info-value">@Model.Gender</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Address</div>
                        <div class="info-value">@Model.Address</div>
                    </div>
                </div>
            </div>

            <!-- License Information -->
            <div class="info-card">
                <div class="card-title">
                    <i class="fas fa-id-badge"></i>
                    License Information
                </div>
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-label">License Number</div>
                        <div class="info-value">@Model.LicenseNumber</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Category</div>
                        <div class="info-value">@Model.LicenseCategory</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Expiry Date</div>
                        <div class="info-value">@(Model.LicenseExpiryDate?.ToString("MMMM dd, yyyy") ?? "Not specified")</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Status</div>
                        <div class="info-value">
                            @if (Model.LicenseExpiryDate.HasValue && daysUntilExpiry <= 0)
                            {
                                <span style="color: #dc3545;">Expired</span>
                            }
                            else if (Model.LicenseExpiryDate.HasValue && daysUntilExpiry <= 30)
                            {
                                <span style="color: #ffc107;">Expiring Soon</span>
                            }
                            else
                            {
                                <span style="color: #28a745;">Valid</span>
                            }
                        </div>
                    </div>
                </div>
            </div>

            <!-- Employment Information -->
            <div class="info-card">
                <div class="card-title">
                    <i class="fas fa-briefcase"></i>
                    Employment Information
                </div>
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-label">Employment Status</div>
                        <div class="info-value">@Model.EmploymentStatus</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Company Branch ID</div>
                        <div class="info-value">@Model.CompanyBranchId</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Shift Status</div>
                        <div class="info-value">@Model.ShiftStatus</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Account Status</div>
                        <div class="info-value">
                            <span style="color: @(Model.IsActive ? "#28a745" : "#dc3545");">
                                @(Model.IsActive ? "Active" : "Inactive")
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="sidebar">
            <!-- Quick Stats -->
            <div class="quick-stats">
                <div class="card-title" style="color: white; border-color: rgba(255,255,255,0.3);">
                    <i class="fas fa-chart-line"></i>
                    Quick Stats
                </div>
                <div class="stat-item">
                    <span>Account Age</span>
                    <strong>@((DateTime.Now - Model.CreatedDate).Days) days</strong>
                </div>
                <div class="stat-item">
                    <span>Documents</span>
                    <strong>@Model.DriverDocuments.Count()</strong>
                </div>
                <div class="stat-item">
                    <span>License Valid</span>
                    <strong>@(Model.LicenseExpiryDate.HasValue && daysUntilExpiry > 0 ? "Yes" : "No")</strong>
                </div>
            </div>

            <!-- Contact Card -->
            <div class="info-card">
                <div class="card-title">
                    <i class="fas fa-address-book"></i>
                    Contact Details
                </div>
                <div class="info-item" style="margin-bottom: 15px;">
                    <div class="info-label">Email</div>
                    <div class="info-value">
                        <a href="mailto:@Model.Email" style="color: #667eea; text-decoration: none;">
                            @Model.Email
                        </a>
                    </div>
                </div>
                <div class="info-item">
                    <div class="info-label">Phone</div>
                    <div class="info-value">
                        <a href="tel:@Model.PhoneNumber" style="color: #667eea; text-decoration: none;">
                            @Model.PhoneNumber
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Documents Section -->
    <div class="info-card documents-section">
        <div class="card-title">
            <i class="fas fa-folder-open"></i>
            Driver Documents (@Model.DriverDocuments.Count())
        </div>

        @if (Model.DriverDocuments.Any())
        {
            <div class="documents-grid">
                @foreach (var doc in Model.DriverDocuments)
                {
                    <div class="document-card">
                        @if (doc.FileName.EndsWith(".jpg") || doc.FileName.EndsWith(".jpeg") ||
                                        doc.FileName.EndsWith(".png") || doc.FileName.EndsWith(".gif"))
                        {
                            <img src="@doc.FileName" alt="@doc.FileName" class="document-image"
                                 onerror="this.parentElement.innerHTML='<div class=\'no-image-placeholder\'><i class=\'fas fa-file-image fa-2x\'></i><br>Image not found</div>'" />
                        }
                        else
                        {
                            <div class="no-image-placeholder">
                                <div style="text-align: center;">
                                    <i class="fas fa-file-alt fa-2x"></i>
                                    <br>Document File
                                </div>
                            </div>
                        }
                        <div class="document-info">
                            <div class="document-name">@doc.FileName</div>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div style="text-align: center; padding: 40px; color: #6c757d;">
                <i class="fas fa-folder-open fa-3x" style="margin-bottom: 15px;"></i>
                <p>No documents uploaded yet.</p>
            </div>
        }
    </div>
</div> *@




@using FleetManager.Business.DataObjects
@model DriverDto
@{
    ViewData["Title"] = "Driver Profile - " + Model.FullName;
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        padding: 20px;
    }

    .profile-container {
        max-width: 1400px;
        margin: 0 auto;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(20px);
        border-radius: 24px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        animation: slideUp 0.8s ease-out;
    }

    @@keyframes slideUp {
        from

    {
        opacity: 0;
        transform: translateY(50px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }

    }

    .profile-hero {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 40px;
        position: relative;
        overflow: hidden;
    }

        .profile-hero::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -10%;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float 6s ease-in-out infinite;
        }

        .profile-hero::after {
            content: '';
            position: absolute;
            bottom: -30%;
            left: -5%;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
            animation: float 8s ease-in-out infinite reverse;
        }

    @@keyframes float {
        0%, 100%

    {
        transform: translateY(0px) rotate(0deg);
    }

    50% {
        transform: translateY(-20px) rotate(10deg);
    }

    }

    .hero-content {
        display: flex;
        align-items: center;
        gap: 40px;
        position: relative;
        z-index: 2;
    }

    .avatar-section {
        position: relative;
        animation: zoomIn 1s ease-out 0.3s both;
    }

    @@keyframes zoomIn {
        from

    {
        opacity: 0;
        transform: scale(0.8);
    }

    to {
        opacity: 1;
        transform: scale(1);
    }

    }

    .avatar-container {
        position: relative;
        width: 160px;
        height: 160px;
        border-radius: 50%;
        background: linear-gradient(45deg, #ff6b6b, #ffd93d, #6bcf7f, #4d9de0);
        padding: 4px;
        animation: rotate 10s linear infinite;
    }

    @@keyframes rotate {
        from

    {
        transform: rotate(0deg);
    }

    to {
        transform: rotate(360deg);
    }

    }

    .avatar {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid white;
        transition: transform 0.3s ease;
    }

        .avatar:hover {
            transform: scale(1.05);
        }

    .status-indicator {
        position: absolute;
        bottom: 10px;
        right: 10px;
        width: 32px;
        height: 32px;
        border-radius: 50%;
        border: 4px solid white;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        animation: pulse 2s infinite;
    }

    .status-active {
        background: linear-gradient(45deg, #56ab2f, #a8e6cf);
    }

    .status-inactive {
        background: linear-gradient(45deg, #ff416c, #ff4b2b);
    }

    @@keyframes pulse {
        0%

    {
        box-shadow: 0 0 0 0 rgba(86, 171, 47, 0.7);
    }

    70% {
        box-shadow: 0 0 0 10px rgba(86, 171, 47, 0);
    }

    100% {
        box-shadow: 0 0 0 0 rgba(86, 171, 47, 0);
    }

    }

    .hero-info {
        flex: 1;
        color: white;
        animation: slideInRight 1s ease-out 0.5s both;
    }

    @@keyframes slideInRight {
        from

    {
        opacity: 0;
        transform: translateX(50px);
    }

    to {
        opacity: 1;
        transform: translateX(0);
    }

    }

    .hero-name {
        font-size: 3rem;
        font-weight: 700;
        margin-bottom: 10px;
        background: linear-gradient(45deg, #fff, #f0f0f0);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .hero-subtitle {
        font-size: 1.2rem;
        opacity: 0.9;
        margin-bottom: 25px;
        font-weight: 300;
    }

    .hero-badges {
        display: flex;
        gap: 15px;
        flex-wrap: wrap;
    }

    .badge {
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(10px);
        padding: 10px 20px;
        border-radius: 25px;
        font-size: 0.9rem;
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.3);
    }

        .badge:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
        }

    .content-wrapper {
        padding: 40px;
    }

    .tabs-container {
        margin-bottom: 30px;
    }

    .tabs {
        display: flex;
        gap: 5px;
        background: #f8fafc;
        padding: 8px;
        border-radius: 16px;
        margin-bottom: 30px;
    }

    .tab {
        flex: 1;
        padding: 15px 25px;
        text-align: center;
        border-radius: 12px;
        cursor: pointer;
        transition: all 0.3s ease;
        font-weight: 600;
        color: #64748b;
        border: none;
        background: transparent;
        position: relative;
    }

        .tab.active {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }

        .tab:not(.active):hover {
            background: white;
            color: #334155;
        }

    .tab-content {
        display: none;
        animation: fadeIn 0.5s ease-out;
    }

        .tab-content.active {
            display: block;
        }

    @@keyframes fadeIn {
        from

    {
        opacity: 0;
        transform: translateY(20px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }

    }

    .info-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 25px;
        margin-bottom: 30px;
    }

    .info-card {
        background: white;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
        border: 1px solid #f1f5f9;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

        .info-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #667eea, #764ba2);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .info-card:hover::before {
            transform: scaleX(1);
        }

        .info-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.12);
        }

    .card-header {
        display: flex;
        align-items: center;
        gap: 15px;
        margin-bottom: 25px;
        padding-bottom: 15px;
        border-bottom: 2px solid #f8fafc;
    }

    .card-icon {
        width: 50px;
        height: 50px;
        border-radius: 15px;
        background: linear-gradient(135deg, #667eea, #764ba2);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1.2rem;
    }

    .card-title {
        font-size: 1.3rem;
        font-weight: 700;
        color: #1e293b;
    }

    .field-group {
        display: grid;
        gap: 20px;
    }

    .field {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .field-label {
        font-size: 0.85rem;
        font-weight: 600;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .field-value {
        font-size: 1.1rem;
        font-weight: 600;
        color: #1e293b;
        padding: 12px 0;
        border-bottom: 1px solid #f1f5f9;
    }

    .documents-gallery {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 25px;
    }

    .document-item {
        background: white;
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
        transition: all 0.3s ease;
        border: 1px solid #f1f5f9;
    }

        .document-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.15);
        }

    .document-preview {
        height: 200px;
        background: #f8fafc;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        overflow: hidden;
    }

    .document-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.3s ease;
    }

    .document-item:hover .document-image {
        transform: scale(1.05);
    }

    .document-placeholder {
        color: #94a3b8;
        font-size: 3rem;
        opacity: 0.5;
    }

    .document-info {
        padding: 20px;
    }

    .document-name {
        font-weight: 600;
        color: #1e293b;
        font-size: 1rem;
        line-height: 1.4;
    }

    .license-status {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 8px 16px;
        border-radius: 20px;
        font-size: 0.9rem;
        font-weight: 600;
    }

    .status-valid {
        background: #dcfce7;
        color: #16a34a;
    }

    .status-warning {
        background: #fef3c7;
        color: #d97706;
    }

    .status-expired {
        background: #fee2e2;
        color: #dc2626;
    }

    .empty-state {
        text-align: center;
        padding: 60px 20px;
        color: #64748b;
    }

    .empty-icon {
        font-size: 4rem;
        margin-bottom: 20px;
        opacity: 0.3;
    }

    .contact-actions {
        display: flex;
        gap: 15px;
        margin-top: 20px;
    }

    .action-btn {
        flex: 1;
        padding: 12px 20px;
        border-radius: 12px;
        border: none;
        font-weight: 600;
        text-decoration: none;
        text-align: center;
        cursor: pointer;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
    }

    .btn-primary {
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: white;
    }

    .btn-secondary {
        background: #f8fafc;
        color: #475569;
        border: 2px solid #e2e8f0;
    }

    .action-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    }

    @@media (max-width: 768px) {
        .hero-content

    {
        flex-direction: column;
        text-align: center;
        gap: 25px;
    }

    .hero-name {
        font-size: 2rem;
    }

    .tabs {
        flex-direction: column;
    }

    .info-grid {
        grid-template-columns: 1fr;
    }

    .hero-badges {
        justify-content: center;
    }

    }
</style>

<div class="profile-container">
    <!-- Hero Section -->
    <div class="profile-hero">
        <div class="hero-content">
            <div class="avatar-section">
                <div class="avatar-container">
                    <img src="@Model.PassportFileName" alt="@Model.FullName" class="avatar"
                         onerror="this.src='data:image/svg+xml;base64,'" />
                    <div class="status-indicator @(Model.IsActive ? "status-active" : "status-inactive")"></div>
                </div>
            </div>
            <div class="hero-info">
                <h1 class="hero-name">@Model.FullName</h1>
                <p class="hero-subtitle">@Model.EmploymentStatus • @Model.LicenseCategory License</p>
                <div class="hero-badges">
                    <div class="badge">
                        <i class="fas fa-envelope"></i>
                        @Model.Email
                    </div>
                    <div class="badge">
                        <i class="fas fa-phone"></i>
                        @Model.PhoneNumber
                    </div>
                    <div class="badge">
                        <i class="fas fa-calendar"></i>
                        Joined @Model.CreatedDate.ToString("MMM yyyy")
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Content Wrapper -->
    <div class="content-wrapper">
        <!-- Tabs -->
        <div class="tabs-container">
            <div class="tabs">
                <button class="tab active" onclick="switchTab(event, 'personal')">
                    <i class="fas fa-user"></i> Personal Info
                </button>
                <button class="tab" onclick="switchTab(event, 'license')">
                    <i class="fas fa-id-card"></i> License Details
                </button>
                <button class="tab" onclick="switchTab(event, 'employment')">
                    <i class="fas fa-briefcase"></i> Employment
                </button>
                <button class="tab" onclick="switchTab(event, 'documents')">
                    <i class="fas fa-folder"></i> Documents (@Model.DriverDocuments.Count())
                </button>
            </div>

            <!-- Personal Info Tab -->
            <div id="personal" class="tab-content active">
                <div class="info-grid">
                    <div class="info-card">
                        <div class="card-header">
                            <div class="card-icon">
                                <i class="fas fa-user"></i>
                            </div>
                            <h3 class="card-title">Basic Information</h3>
                        </div>
                        <div class="field-group">
                            <div class="field">
                                <div class="field-label">Full Name</div>
                                <div class="field-value">@Model.FullName</div>
                            </div>
                            <div class="field">
                                <div class="field-label">Date of Birth</div>
                                <div class="field-value">@Model.DateOfBirth.ToString("MMMM dd, yyyy")</div>
                            </div>
                            <div class="field">
                                <div class="field-label">Gender</div>
                                <div class="field-value">@Model.Gender</div>
                            </div>
                        </div>
                    </div>

                    <div class="info-card">
                        <div class="card-header">
                            <div class="card-icon">
                                <i class="fas fa-map-marker-alt"></i>
                            </div>
                            <h3 class="card-title">Contact Information</h3>
                        </div>
                        <div class="field-group">
                            <div class="field">
                                <div class="field-label">Email Address</div>
                                <div class="field-value">@Model.Email</div>
                            </div>
                            <div class="field">
                                <div class="field-label">Phone Number</div>
                                <div class="field-value">@Model.PhoneNumber</div>
                            </div>
                            <div class="field">
                                <div class="field-label">Address</div>
                                <div class="field-value">@Model.Address</div>
                            </div>
                        </div>
                        <div class="contact-actions">
                            <a href="mailto:@Model.Email" class="action-btn btn-primary">
                                <i class="fas fa-envelope"></i>
                                Send Email
                            </a>
                            <a href="tel:@Model.PhoneNumber" class="action-btn btn-secondary">
                                <i class="fas fa-phone"></i>
                                Call Now
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- License Details Tab -->
            <div id="license" class="tab-content">
                <div class="info-grid">
                    <div class="info-card">
                        <div class="card-header">
                            <div class="card-icon">
                                <i class="fas fa-id-card"></i>
                            </div>
                            <h3 class="card-title">License Information</h3>
                        </div>
                        <div class="field-group">
                            <div class="field">
                                <div class="field-label">License Number</div>
                                <div class="field-value">@Model.LicenseNumber</div>
                            </div>
                            <div class="field">
                                <div class="field-label">License Category</div>
                                <div class="field-value">@Model.LicenseCategory</div>
                            </div>
                            <div class="field">
                                <div class="field-label">Expiry Date</div>
                                <div class="field-value">@(Model.LicenseExpiryDate?.ToString("MMMM dd, yyyy") ?? "Not specified")</div>
                            </div>
                            <div class="field">
                                <div class="field-label">License Status</div>
                                <div class="field-value">
                                    @{
                                        var daysUntilExpiry = Model.LicenseExpiryDate.HasValue ?
                                        (Model.LicenseExpiryDate.Value - DateTime.Now).TotalDays : 0;

                                        string statusClass = "status-valid";
                                        string statusText = "Valid";
                                        string statusIcon = "fa-check-circle";

                                        if (Model.LicenseExpiryDate.HasValue && daysUntilExpiry <= 0)
                                        {
                                            statusClass = "status-expired";
                                            statusText = "Expired";
                                            statusIcon = "fa-times-circle";
                                        }
                                        else if (Model.LicenseExpiryDate.HasValue && daysUntilExpiry <= 30)
                                        {
                                            statusClass = "status-warning";
                                            statusText = $"Expires in {Math.Ceiling(daysUntilExpiry)} days";
                                            statusIcon = "fa-exclamation-circle";
                                        }
                                    }
                                    <span class="license-status @statusClass">
                                        <i class="fas @statusIcon"></i>
                                        @statusText
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Employment Tab -->
            <div id="employment" class="tab-content">
                <div class="info-grid">
                    <div class="info-card">
                        <div class="card-header">
                            <div class="card-icon">
                                <i class="fas fa-briefcase"></i>
                            </div>
                            <h3 class="card-title">Employment Details</h3>
                        </div>
                        <div class="field-group">
                            <div class="field">
                                <div class="field-label">Employment Status</div>
                                <div class="field-value">@Model.EmploymentStatus</div>
                            </div>
                            <div class="field">
                                <div class="field-label">Company Branch ID</div>
                                <div class="field-value">#@Model.CompanyBranchId</div>
                            </div>
                            <div class="field">
                                <div class="field-label">Shift Status</div>
                                <div class="field-value">@Model.ShiftStatus</div>
                            </div>
                            <div class="field">
                                <div class="field-label">Account Status</div>
                                <div class="field-value">
                                    <span class="license-status @(Model.IsActive ? "status-valid" : "status-expired")">
                                        <i class="fas @(Model.IsActive ? "fa-check-circle" : "fa-times-circle")"></i>
                                        @(Model.IsActive ? "Active" : "Inactive")
                                    </span>
                                </div>
                            </div>
                            <div class="field">
                                <div class="field-label">Account Created</div>
                                <div class="field-value">@Model.CreatedDate.ToString("MMMM dd, yyyy")</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Documents Tab -->
            <div id="documents" class="tab-content">
                @if (Model.DriverDocuments.Any())
                {
                    <div class="documents-gallery">
                        @foreach (var doc in Model.DriverDocuments)
                        {
                            <div class="document-item">
                                <div class="document-preview">
                                    @if (doc.FileName.EndsWith(".jpg") || doc.FileName.EndsWith(".jpeg") ||
                                                                doc.FileName.EndsWith(".png") || doc.FileName.EndsWith(".gif"))
                                    {
                                        <img src="@doc.FileName" alt="@doc.FileName" class="document-image"
                                             onerror="this.parentElement.innerHTML='<i class=\'fas fa-file-image document-placeholder\'></i>'" />
                                    }
                                    else
                                    {
                                        <i class="fas fa-file-alt document-placeholder"></i>
                                    }
                                </div>
                                <div class="document-info">
                                    <div class="document-name">@doc.FileName</div>
                                </div>
                            </div>
                        }
                    </div>
                }
                else
                {
                    <div class="empty-state">
                        <i class="fas fa-folder-open empty-icon"></i>
                        <h3>No Documents Available</h3>
                        <p>No documents have been uploaded for this driver yet.</p>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<script>
    function switchTab(event, tabName) {
        // Hide all tab contents
        const tabContents = document.querySelectorAll('.tab-content');
        tabContents.forEach(content => {
            content.classList.remove('active');
        });

        // Remove active class from all tabs
        const tabs = document.querySelectorAll('.tab');
        tabs.forEach(tab => {
            tab.classList.remove('active');
        });

        // Show selected tab content and mark tab as active
        document.getElementById(tabName).classList.add('active');
        event.currentTarget.classList.add('active');
    }

    // Add smooth scroll behavior and interactive elements
    document.addEventListener('DOMContentLoaded', function() {
        // Add click handlers for contact actions
        const emailBtn = document.querySelector('a[href^="mailto:"]');
        const phoneBtn = document.querySelector('a[href^="tel:"]');

        if (emailBtn) {
            emailBtn.addEventListener('click', function(e) {
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 150);
            });
        }

        if (phoneBtn) {
            phoneBtn.addEventListener('click', function(e) {
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 150);
            });
        }
    });
</script>