@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger alert-dismissible fade show mb-4" role="alert">
        <h6 class="alert-heading mb-1 fw-semibold">Please fix the following before saving:</h6>
        <ul class="mb-0 ps-3">
            @foreach (var (key, state) in ViewData.ModelState)
            {
                foreach (var error in state.Errors)
                {
                    
                    <li><strong>[@key]</strong> @error.ErrorMessage</li>
                }
            }
        </ul>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
















@* @if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger alert-dismissible fade show mb-4" role="alert">
        <div class="d-flex align-items-start gap-2">
            <i class="feather-alert-circle mt-1 flex-shrink-0"></i>
            <div class="flex-grow-1">
                <h6 class="alert-heading mb-1 fw-semibold">
                    Please fix the following before saving:
                </h6>
                <ul class="mb-0 ps-3">
                    @foreach (var (key, state) in ViewData.ModelState)
                    {
                        foreach (var error in state.Errors)
                        {
                            <li>@error.ErrorMessage</li>
                        }
                    }
                </ul>
            </div>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
 *@